---
id: documentation
title: Documentação — PW2 Shop
---

# 🛒 Lojinha — Documentação Oficial

Este documento explica **como executar a aplicação completa** (backend + frontend + PostgreSQL via Docker)  
e descreve de forma clara o **fluxo de cadastro de produtos**, incluindo o que acontece em cada camada  
(**controller → useCase → repository → banco**).

---

# 🚀 Como rodar a aplicação

A aplicação é dividida em três partes:

- **Backend** — Node.js + Express + Drizzle ORM
- **Frontend** — Next.js 16
- **Banco de Dados** — PostgreSQL via Docker

Toda a stack sobe com **um único comando Docker Compose**.

---

# 📦 1. Clonar o projeto

```bash
git clone https://github.com/seu-repo/pw2.git
cd pw2
```
