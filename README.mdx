# 🛒 Lojinha — Documentação

---

# 🚀 Como rodar a aplicação

A aplicação é dividida em três partes:

- **Backend** — Node.js + Express + Drizzle ORM
- **Frontend** — Next.js 16
- **Banco de Dados** — PostgreSQL via Docker

Toda a stack sobe com **um único comando Docker Compose**.

---

# 📦 1. Clonar o projeto

```bash
git clone https://github.com/seu-repo/pw2.git
cd pw2
```

---

# 🐳 2. Subir banco + backend + frontend

No diretório raiz:

```bash
sudo docker compose up -d --build`

```

Isso irá:

Criar o PostgreSQL

Subir o backend

Subir o frontend

Acessos:

Frontend: http://localhost:3000

Backend: http://localhost:3333

---

Para parar tudo:

```bash
sudo docker compose down `

```
